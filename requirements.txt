# Runtime deps for the Streamlit app.
# Tested with Python 3.12; keep top-level packages only so pip can resolve
# current, compatible transitive dependencies.
streamlit>=1.33,<2
numpy>=1.26
pandas>=2.2
scipy>=1.13
matplotlib>=3.8
